//列表区展示的组件  与  容器内放置组件类型上的映射关系
const typeArray = [
    ['DBox', {
        initW: 100,
        initH: 100
    }],
    ['DInput', {
        initW: 200,
        initH: 80
    }]
]

export const TYPE_INIT_INFO_MAP = new Map(typeArray)